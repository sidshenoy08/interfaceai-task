OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# Mock target app
TARGET_APP_PORT=4000

# Operator console (human handoff)
OPERATOR_PORT=4001

# Set to "true" to run the browser headed (visible) instead of headless
HEADED=true
